#!/bin/sh

# This loop will update the mpd statusbar module whenever a command changes the
# music player's status. mpd must be running on start for this to work.

while : ; do
	mpc idle >/dev/null && pkill -SIGRTMIN+10 someblocks || break
done
